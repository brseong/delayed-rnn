seed: 0
num_epochs: 100
batch_size: 512
num_workers: 0
lr: 0.001
use_lr_scheduler: true
use_wandb: true
wandb:
  entity: CIDA
  group_name: LSTM
model_args:
  num_layers: 1
  hidden_size: 256
dataset:
  _target_: datasets.Qswap.Qswap
  one_hot_dim: 10
  min_seq_len: 10
  max_seq_len: 20
model:
  _target_: models.LSTM.LSTM
